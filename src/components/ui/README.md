# UI Components Folder

This folder contains low-level base UI components (e.g., Buttons, Modals, Inputs) that are completely reusable across the application.

Most components here will be added via shadcn/ui.
